package com.compx.utils;

import java.io.IOException;
import java.io.InputStream;
import java.util.List;

import com.fasterxml.jackson.databind.ObjectReader;

public class CsvUtils {
	/*
	 * private static final CsvMapper mapper = new CsvMapper(); public static <T>
	 * List<T> read(Class<T> clazz, InputStream stream) throws IOException {
	 * CsvSchema schema =
	 * mapper.schemaFor(clazz).withHeader().withColumnReordering(true); ObjectReader
	 * reader = mapper.readerFor(clazz).with(schema); return
	 * reader.<T>readValues(stream).readAll(); }
	 */
}
